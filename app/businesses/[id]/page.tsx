"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

type Merchant={id:string;business_name:string;city:string|null;state:string|null;description:string|null;is_approved:boolean;is_active:boolean};
type Coupon={id:string;title:string;description:string|null;discount_text:string;expires_at:string|null};
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL;
const KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

export default function MerchantProfilePage({params}:{params:Promise<{id:string}>}){
 const[merchant,setMerchant]=useState<Merchant|null>(null);const[coupons,setCoupons]=useState<Coupon[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");
 useEffect(()=>{(async()=>{try{const{id}=await params;if(!URL||!KEY)throw new Error("Merchant information is temporarily unavailable.");const headers={apikey:KEY};const mr=await fetch(`${URL}/rest/v1/merchants?id=eq.${encodeURIComponent(id)}&select=id,business_name,city,state,description,is_approved,is_active&limit=1`,{headers,cache:"no-store"});if(!mr.ok)throw new Error("Business profile unavailable.");const rows=await mr.json();const item=rows[0];if(!item||!item.is_approved||!item.is_active)throw new Error("This business profile is not currently public.");setMerchant(item);const cr=await fetch(`${URL}/rest/v1/coupons?merchant_id=eq.${encodeURIComponent(id)}&status=eq.active&select=id,title,description,discount_text,expires_at&order=created_at.desc`,{headers,cache:"no-store"});if(cr.ok){const now=Date.now();const items=await cr.json();setCoupons(items.filter((c:Coupon)=>!c.expires_at||new Date(c.expires_at).getTime()>now));}}catch(e){setError(e instanceof Error?e.message:"We couldn't load this business right now.")}finally{setLoading(false)}})()},[params]);
 if(loading)return <main className="queen-page"><div className="loading">♛ Loading business profile…</div><style jsx>{`.loading{min-height:70vh;display:grid;place-items:center;font-weight:900;color:var(--queen-espresso)}`}</style></main>;
 if(error||!merchant)return <main className="queen-page"><section className="empty"><div className="icon">👑</div><h1>Business Not Found</h1><p>{error||"This business profile is unavailable."}</p><Link href="/coupons" className="queen-button primary-button">Back to Deal Vault →</Link></section><style jsx>{`.empty{max-width:720px;margin:90px auto;text-align:center;padding:40px 20px}.icon{font-size:48px}.empty h1{margin:15px 0 8px}.empty p{color:var(--queen-muted);margin-bottom:25px}`}</style></main>;
 const location=[merchant.city,merchant.state].filter(Boolean).join(", ")||"Nationwide";
 return <main className="queen-page"><header className="site-header"><Link href="/" className="brand-wrap" aria-label="Coupon Queen home"><div className="brand-crown">♕</div><div><div className="brand-name">COUPON QUEEN</div><div className="brand-tagline">The Crown Jewel of Savings</div></div></Link><nav><Link href="/coupons">Coupons</Link><Link href="/businesses">Businesses</Link></nav><div className="header-sparkle">✦</div></header><section className="profile-hero"><div className="crest">♛</div><div className="section-kicker">♢ BUSINESS KINGDOM PROFILE ♢</div><h1>{merchant.business_name}</h1><p>{merchant.description||"Discover current savings from this Coupon Queen merchant."}</p><div className="location">📍 {location}</div></section><section className="deals"><div className="section-heading"><div className="section-kicker">✦ CURRENT OFFERS ✦</div><h2>Deals from {merchant.business_name}</h2><p>{coupons.length?`${coupons.length} live ${coupons.length===1?"offer":"offers"} are available now.`:"There are no live offers from this merchant right now."}</p></div>{coupons.length?<div className="grid">{coupons.map(c=><article className="card" key={c.id}><div className="discount">{c.discount_text}</div><h3>{c.title}</h3><p>{c.description||"A live offer from the Coupon Queen Business Kingdom."}</p><span className="expires">{c.expires_at?`Ends ${new Date(c.expires_at).toLocaleDateString()}`:"Live offer"}</span><Link href={`/coupons/${c.id}`} className="deal-link">Get Deal →</Link></article>)}</div>:<div className="empty-deals"><div>💎</div><h3>No live deals right now</h3><p>Check back soon for new offers.</p><Link href="/coupons" className="deal-link">Explore the Deal Vault →</Link></div>}</section><footer className="site-footer"><Link href="/" className="footer-brand"><span>♕</span>COUPON QUEEN</Link><div className="footer-tagline">The Crown Jewel of Savings</div></footer><style jsx>{`.profile-hero{max-width:850px;margin:0 auto;padding:70px 24px 55px;text-align:center}.crest{font-size:48px;color:var(--queen-gold)}.profile-hero h1{margin:10px 0 14px;color:var(--queen-espresso);font-size:clamp(40px,7vw,68px)}.profile-hero p{max-width:700px;margin:0 auto;color:var(--queen-muted);font-size:16px;line-height:1.75}.location{display:inline-block;margin-top:22px;padding:12px 16px;border:1px solid var(--queen-border);border-radius:14px;background:white;color:var(--queen-espresso);font-weight:850}.deals{max-width:1120px;margin:0 auto;padding:20px 24px 80px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px}.card{padding:26px;border:1px solid var(--queen-border);border-radius:22px;background:white;box-shadow:0 14px 34px rgba(47,35,24,.07)}.discount{color:var(--queen-gold);font-size:18px;font-weight:950}.card h3{margin:9px 0 8px;color:var(--queen-espresso);font-size:21px}.card p{color:var(--queen-muted);line-height:1.6}.expires{display:block;margin-top:16px;color:var(--queen-muted);font-size:12px;font-weight:800}.deal-link{display:inline-block;margin-top:18px;color:var(--queen-turquoise-dark);font-weight:900;text-decoration:none}.empty-deals{text-align:center;padding:45px 20px;border:1px dashed var(--queen-border);border-radius:22px;background:white}.empty-deals>div{font-size:42px}.empty-deals p{color:var(--queen-muted)}@media(max-width:820px){.grid{grid-template-columns:1fr 1fr}}@media(max-width:560px){.grid{grid-template-columns:1fr}.profile-hero{padding-top:48px}}`}</style></main>;
}
